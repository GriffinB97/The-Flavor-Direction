<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="public/css/stylesheet.css">
    <title>Flavor Direction</title>
</head>
<body>
    <header>
        <h1>Flavor Direction</h1>
        <h2> One Stop Shop for Everything Delicious!</h2>
    </header>
    {{#if logged_in}}
    <!-- Need the / tag for the pages to redirect too -->
    <nav>   
        <a href="/">Home</a>      
        <a href="/recipe">Create Recipe</a>      
        <button class="btn" id="logout">Logout</button>
        </nav>
    {{else}}
     <nav>
        <a href="/login">Login</a>
    </nav>
    {{/if}}
    
    <main>
        {{{body}}}
    </main>
    
     {{#if loggedIn}}
        <script src="../../public/js/logout.js"></script>
     {{/if}}
</body>
</html>